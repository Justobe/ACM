霍夫曼编码有两种出题的方法：
1.计算权值
将数值存入优先队列（从小到大），
然后取队列定两个，sum = sum + a + b；重新存入a+b；
循环直到队列大小为1
2.输出霍夫曼编码前缀
构造数据结构存储字符，存储该字符父节点index，存储该节点在数组中的index，存储该字符权值
按照权值从大到小存入优先队列
每次弹出队列前两个，生成新的字符，存入队列
循环直到队列大小为1


*注意处理个数为1的情况
*优先队列的排序是不稳定的


